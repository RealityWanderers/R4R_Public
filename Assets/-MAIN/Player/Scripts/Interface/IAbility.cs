public interface IAbility
{
    void DisableAbility();  // Disables the ability (turns off functionality)
    void EnableAbility();   // Enables the ability (activates functionality)
    void ResetAbility();    // Resets the internal state of the ability
}
