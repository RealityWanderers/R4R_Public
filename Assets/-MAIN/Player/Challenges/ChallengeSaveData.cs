using UnityEngine;

[CreateAssetMenu(fileName = "NewChallengeSaveData", menuName = "Challenge/ChallengeSaveData")]
public class ChallengeSaveData : ScriptableObject
{
    //public float bestTime = -1f; // Use -1 as "unset" or "no record"

    //public enum Rank {None, D, C, B, A, S }
    //public Rank bestRank = Rank.None;

    //public enum ChallengeType { Delivery}
    //public ChallengeType challengeType = ChallengeType.Delivery;
}
